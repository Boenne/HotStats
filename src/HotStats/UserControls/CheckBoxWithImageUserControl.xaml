<UserControl x:Class="HotStats.UserControls.CheckBoxWithImageUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotStats.UserControls"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:command="http://www.galasoft.ch/mvvmlight"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="60">
    <StackPanel Cursor="Hand">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseDown">
                <command:EventToCommand
                    Command="{Binding Mode=OneWay, Path=MouseDownMaster}" PassEventArgsToCommand="False" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Image HorizontalAlignment="Center" Source="{Binding ImagePath}" ToolTip="Warcraft" Height="64" Width="50"></Image>
        <CheckBox HorizontalAlignment="Center" IsChecked="True" Name="Check"></CheckBox>
    </StackPanel>
</UserControl>
