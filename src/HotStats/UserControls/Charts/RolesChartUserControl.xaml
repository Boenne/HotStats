<UserControl x:Class="HotStats.UserControls.Charts.RolesChartUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HotStats.UserControls.Charts"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
             DataContext="{Binding RolesChartViewModel, Source={StaticResource ViewModelLocator}}">
    <!--<wpf:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Left">
            <wpf:CartesianChart.AxisX>
                <wpf:Axis Title="Roles" Labels="{Binding Labels}"></wpf:Axis>
            </wpf:CartesianChart.AxisX>
            <wpf:CartesianChart.AxisY>
                <wpf:Axis Title="Games played"></wpf:Axis>
            </wpf:CartesianChart.AxisY>
        </wpf:CartesianChart>-->
    <wpf:PieChart Series="{Binding SeriesCollection}" LegendLocation="Left" Height="300" Width="426" />
</UserControl>